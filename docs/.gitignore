/.quarto/
**/*.quarto_ipynb
**/*.quarto_ipynb_*
_site/
**/*_files/
# Stray Quarto render artefacts that can land next to sources when rendering
# individual pages/listings (the real site builds to _site/).
reference/**/*.html
reference/**/*-listing.json
site_libs/
search.json
# Large machine-readable docstring-inventory manifest (regenerated on demand;
# the reviewable .md snapshot is versioned).
_docstring_inventory.json
