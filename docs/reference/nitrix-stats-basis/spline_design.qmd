---
title: "spline_design"
generated: true
description: "Re-evaluate a `SplineBasis` design at new covariate values."
---

[‹ back to `nitrix.stats.basis`](../nitrix-stats-basis.qmd)

<!-- nitrix:gen sig:spline_design -->
```python
spline_design(basis: SplineBasis, x: Float[Array, ' m']) -> Float[Array, 'm k']
```
<!-- nitrix:/gen -->

<!-- nitrix:gen overview:spline_design -->
Re-evaluate a `SplineBasis` design at new covariate values.

Used to render a smooth's partial effect on a fresh grid.  Applies the same
feature map and sum-to-zero constraint as the original construction.
<!-- nitrix:/gen -->

<!-- nitrix:gen params:spline_design -->
**Parameters**

| parameter | type | shape | default | description |
|------------|--------|--------|----------|--------------------------------------------------------------|
| `basis` | `SplineBasis` | — | — | The `SplineBasis` to re-evaluate. |
| `x` | `Float` | `m` | — | `(m,)` covariate values. |

**Returns** `Float` — shape `m` × `k`. The `(m, k)` design matrix at `x` (post-constraint).
<!-- nitrix:/gen -->


<!-- nitrix:gen dims:spline_design -->
**Tensor dimensions**

| dim | meaning |
|---|---|
| `m` | rows of a multi-row contrast |
| `k` | kernel or basis components |
<!-- nitrix:/gen -->
